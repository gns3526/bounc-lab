# BOUNC LAB 앱인토스 출시 준비

이 문서는 BOUNC LAB을 토스 앱인토스의 **게임 미니앱**으로 제출하기 위한 인수인계 문서입니다. 코드로 자동화할 수 있는 준비와, 운영자 또는 앱인토스 콘솔에서 직접 완료해야 하는 작업을 구분합니다.

공식 기준은 제출 직전에 다시 확인하세요.

- [게임 출시 가이드](https://developers-apps-in-toss.toss.im/checklist/app-game.html)
- [미니앱 등록하기](https://developers-apps-in-toss.toss.im/guide/operation/console-workspace.html)
- [미니앱 출시하기](https://developers-apps-in-toss.toss.im/guide/operation/deploy.html)
- [UI/UX 가이드](https://developers-apps-in-toss.toss.im/design/consumer-ux-guide.html)
- [게임 등급분류 안내](https://toss.im/apps-in-toss/blog/game_rating_classification)

## 1. 현재 코드로 준비된 항목

- 앱인토스 WebView SDK 3.x 의존성과 Vite 기반 토스 빌드 스캐폴드가 포함되어 있습니다.
- 앱인토스 콘솔의 변경 불가능한 `appName`은 `penguin-bounce`로 확정되어 빌드 기본값에 반영되어 있습니다. 운영 빌드에서 `TOSS_APP_NAME`을 명시한다면 반드시 이 값과 같아야 합니다.
- 토스 빌드에서는 `VITE_API_BASE_URL`을 클라이언트에 주입하며, 운영 빌드는 절대 `https://` API 주소만 허용하도록 구성합니다. `VITE_PUBLIC_APP_URL`을 설정하면 토스 밖의 브라우저 공유 대체 경로에서 공개 웹 주소를 사용합니다. 토스 안의 공유는 계속 `intoss://penguin-bounce` 링크를 우선 사용합니다.
- 토스 빌드는 `VITE_TOSS_INTERSTITIAL_AD_GROUP_ID`를 `toss-interstitial-ad-group-id` 메타 태그로 주입합니다. 운영 빌드는 앱인토스 콘솔에서 발급한 운영용 전면 광고 그룹 ID가 반드시 필요하며, 테스트 ID와 예시값은 출시 검증 단계에서 거부됩니다.
- 기존 100개 스테이지, 모바일 조작, 반응형 화면, 맵 에디터, 온라인 맵 탐색·검색·공유 흐름이 구현되어 있습니다.
- 제작자가 직접 클리어한 맵만 게시할 수 있도록 서버가 120Hz 결정론적 물리 리플레이를 다시 검증합니다.
- 게시 티켓은 작성자와 맵 내용에 묶이고 한 번만 사용할 수 있습니다. 입력 크기 제한, 맵 스키마 검증, 요청 속도 제한과 기본 보안 헤더도 적용되어 있습니다.
- 온라인 맵은 `DATA_FILE`이 가리키는 JSON 파일에 원자적으로 저장하며 서버 재시작 뒤에도 다시 불러옵니다.
- SDK 3 라이브·QR 테스트 Origin과 `ALLOWED_ORIGINS` 추가 Origin을 대상으로 백엔드 CORS를 설정할 수 있습니다.
- 자동 테스트는 `npm test`로 실행할 수 있습니다.

위 항목은 제출 자격 자체를 뜻하지 않습니다. 아래의 콘솔 정보, 이미지, 등급분류 및 토스 앱 QR 테스트가 끝나야 검토를 요청할 수 있습니다.

## 2. 운영자가 먼저 확정해야 하는 값

### 2-1. `appName` 최종 확정 — 필수

`appName`은 `intoss://{appName}` 딥링크와 배포 Origin에 쓰이는 고유 ID이며, 콘솔에 등록한 뒤에는 수정하거나 삭제할 수 없습니다.

- 확정값: `penguin-bounce`
- 콘솔, `TOSS_APP_NAME`, `apps-in-toss.config.ts`에서 철자가 완전히 동일한지 확인합니다.
- `TOSS_APP_NAME`을 생략하면 빌드는 확정값 `penguin-bounce`를 사용합니다.
- 운영 빌드에 다른 `TOSS_APP_NAME`을 명시하면 출시 검증이 실패합니다.

### 2-2. 운영 HTTPS API URL — 필수

토스는 클라이언트 번들을 호스팅하지만 온라인 맵 API 서버는 별도로 운영해야 합니다.

- 예: `https://api.example.com`
- 유효한 TLS 인증서가 있는 공개 `https://` 주소여야 합니다.
- `GET https://api.example.com/api/health`가 외부에서 성공해야 합니다.
- 운영 빌드 전에 `VITE_API_BASE_URL`에 확정 주소를 넣습니다.
- 토스 외부 브라우저에서도 같은 빌드를 열 수 있다면 `VITE_PUBLIC_APP_URL`에 공개 웹 주소를 넣습니다. Cloudflare Workers에서 웹과 API를 함께 제공하면 API 주소와 같아도 됩니다.
- API 서버의 `ALLOWED_ORIGINS`에 실제 앱과 QR 테스트 Origin을 모두 허용합니다.
- API 키, `PUBLISH_SECRET`, 인증서 같은 비밀값은 클라이언트 번들이나 Git에 넣지 않습니다.

SDK 3 기준 기본 Origin은 다음과 같습니다.

```text
https://<appName>.web.tossmini.com
https://<appName>.private-web.tossmini.com
```

### 2-3. 고객문의 이메일 — 필수

앱인토스 콘솔의 앱 정보에 실제로 응답 가능한 고객문의 이메일을 입력해야 합니다.

- 개인 메일보다 서비스 전용 주소를 권장합니다.
- 맵 신고·삭제 요청, 데이터 삭제 요청, 장애 문의에 대응할 수 있어야 합니다.
- 이용약관과 개인정보처리방침에도 같은 연락처를 표시합니다.

### 2-4. 운영용 전면 광고 그룹 ID — 필수

토스 미니앱 배포판에서만 Apps in Toss 통합 광고를 사용합니다. 앱인토스 콘솔에서 펭귄 바운스용 **운영 전면 광고 그룹**을 만들고 발급된 ID를 빌드 환경의 `VITE_TOSS_INTERSTITIAL_AD_GROUP_ID`에 설정합니다.

- `npm run build:toss`는 ID 누락·공백, `ait-ad-test-interstitial-id` 같은 테스트 ID, `replace-with...` 같은 예시값을 거부합니다.
- `npm run build:toss:dev`는 변수를 비워 두면 `ait-ad-test-interstitial-id`를 자동 사용합니다. 개발·QR 확인용 테스트 ID를 운영 번들에 넣지 않습니다.
- Vite가 토스 HTML에 `<meta name="toss-interstitial-ad-group-id" ...>`를 자동 주입하므로 HTML을 수동 수정하지 않습니다.
- 광고 그룹 ID는 공개 클라이언트 설정값입니다. 광고 콘솔 계정 비밀번호·정산정보·서버 비밀값은 `.env`나 Git에 넣지 않습니다.
- 개인정보처리방침과 앱인토스 콘솔의 광고 관련 고지는 토스 미니앱에서 Apps in Toss 통합 광고(Toss Ads/Google AdMob)를 사용한다는 실제 동작과 일치시킵니다.
- Google Play·ONEstore용 Android 앱과 일반 웹 배포판에는 광고 SDK가 없으므로 Android 스토어의 광고 여부를 토스 미니앱과 혼동하지 않습니다.

## 3. 콘솔 제출 자료

### 3-1. 600×600 앱 로고 — 필수

`release-assets/README.md`의 체크리스트에 따라 PNG를 준비합니다.

- 정확히 600×600px인 PNG
- 모서리를 둥글게 만들지 않은 정사각형
- 배경색 필수, 투명 배경 금지
- 작은 화면과 라이트·다크 모드에서 모두 식별 가능
- 토스 제공 아이콘·이미지를 앱 로고로 사용하거나 2차 가공하지 않음
- SDK 3.x에서는 로고를 빌드 설정이 아니라 앱인토스 콘솔에서 관리함

### 3-2. 1932×828 게임 썸네일 — 필수

- 정확히 1932×828px인 PNG
- 실제 핵심 플레이 장면이 한눈에 보여야 함
- 저해상도 확대, 블러 또는 빈 공간 채우기 금지
- 텍스트는 로고나 짧은 문구 정도로 최소화
- 비속어·정치적 표현·권리 문제가 있는 이미지 금지

### 3-3. 게임 등급분류 증빙 — 필수·외부 절차

게임은 토스의 이용 가능 연령과 별개로 정식 출시 전에 게임 등급분류 증빙이 필요합니다. 다음 중 하나를 준비합니다.

1. App Store, Google Play, ONEstore 또는 Microsoft Store에 실제 출시된 동일 게임의 URL과 자체등급분류 게임물 정보
2. 게임물관리위원회에서 발급받은 등급분류증명서 PDF

스토어 경로를 쓰면 스토어판과 앱인토스판의 원본 플레이 화면을 각각 2장 제출하고, 등록자명·등급분류번호·이용등급·내용정보 등이 공식 조회 결과와 일치해야 합니다. 아직 증빙이 없다면 코드 완성 여부와 관계없이 출시 검토를 끝낼 수 없습니다.

현재 Google Play 출시판은 게임물관리위원회 자체등급분류 조회에서 다음과 같이 확인되었습니다.

- 스토어 링크: `https://play.google.com/store/apps/details?id=com.jellysnow.penguinbounce`
- 등급분류번호: `GOOG-SG-260811-0324`
- 등급분류일자: 2026-08-11
- 이용등급: 전체 이용가
- 내용정보: 폭력성
- 자체등급분류사업자명: 구글

## 4. 백엔드 운영 제약

### 영속 볼륨과 단일 replica가 필수입니다

현재 저장소는 PostgreSQL 같은 공유 데이터베이스가 아니라 프로세스가 관리하는 JSON 파일입니다. 따라서 현재 구조는 다음 조건에서만 안전합니다.

- `DATA_FILE`을 컨테이너의 임시 파일시스템이 아닌 **영속 볼륨의 절대경로**로 설정합니다.
- 맵 파일과 볼륨 스냅샷을 정기적으로 백업합니다.
- 서버는 반드시 **단일 replica**로 운영합니다.
- 수평 확장, 여러 인스턴스가 같은 파일을 공유하는 배포, 요청마다 인스턴스가 달라지는 서버리스 배포는 사용하지 않습니다.

이 제약이 필요한 이유는 다음과 같습니다.

- 맵 상태와 파일 쓰기 잠금은 한 Node.js 프로세스 안에서만 조정됩니다.
- 진행 중인 클리어 시도와 속도 제한 상태는 메모리에 있습니다. 재시작하면 진행 중인 시도는 사라지지만 이미 게시된 맵은 영속 파일에서 복구됩니다.
- 여러 replica를 사용하면 클리어 시작과 완료 요청이 서로 다른 프로세스로 갈 수 있고, 카운터와 파일 상태가 충돌할 수 있습니다.

다중 replica가 필요해지기 전에 맵·게시 티켓·진행 중 시도를 PostgreSQL 같은 공유 DB와 트랜잭션으로 이전하고, 속도 제한도 공유 저장소로 옮겨야 합니다.

운영 환경의 최소 설정 예시는 [.env.toss.example](./.env.toss.example)을 참고합니다. `PUBLISH_SECRET`도 서버 재시작 때 바뀌지 않는 충분히 긴 비밀값으로 별도 설정해야 합니다.

## 5. 토스 앱 QR 실기기 테스트 — 필수

`.ait` 번들을 콘솔에 올린 뒤 생성되는 QR로 토스 앱에서 최소 1회 테스트해야 검토 요청 버튼이 활성화됩니다. 브라우저나 로컬 서버 테스트만으로 대체할 수 없습니다.

다음 흐름을 iOS와 Android 실기기에서 확인합니다.

- 최초 화면이 10초 안에 열림
- 선택한 화면 방향에서 풀스크린으로 보이며 페이지 스크롤, 웹뷰 여백, 레터박스가 없음
- 노치·Dynamic Island·홈 인디케이터와 토스 우측 상단 닫기 버튼이 게임 UI를 가리지 않음
- 토스 닫기·뒤로가기 동작과 게임 종료 확인 모달이 정상 작동함
- 앱을 백그라운드로 보내면 사운드가 멈추고 복귀하면 설정에 맞게 재생됨
- 공식 스테이지 시작·실패·재시작·클리어가 정상 작동함
- 맵 제작 → 검증 플레이 → 직접 클리어 → 게시가 정상 작동함
- 게시한 맵 검색·불러오기·플레이·공유 링크가 정상 작동함
- 운영 API CORS, 오류 안내, 느린 네트워크와 연결 복구가 정상 작동함
- 서버 재시작 뒤 게시 맵이 유지되는지 확인함
- 테스트 광고가 게임 플레이를 방해하지 않는 시점에만 열리고, 표시·닫기·표시 실패 뒤에도 게임 진행과 사운드가 정상 복구되는지 확인함

샌드박스에서 지원되지 않거나 실제 환경과 다른 기능이 있으므로 최종 판단은 반드시 콘솔 QR과 최신 토스 앱에서 합니다.

## 6. 권장 제출 순서

1. 한글 앱 이름을 확정하고, 변경 불가능한 `appName`이 `penguin-bounce`인지 콘솔에서 확인합니다.
2. 고객문의 이메일, 이용약관, 개인정보처리방침의 운영자 정보를 확정합니다.
3. 600×600 로고와 1932×828 썸네일을 제작합니다.
4. 게임 등급분류 증빙을 확보합니다.
5. 영속 볼륨을 연결한 단일 replica API를 HTTPS로 배포하고 백업을 설정합니다.
6. 앱인토스 콘솔에서 운영용 전면 광고 그룹 ID를 발급하고, [.env.toss.example](./.env.toss.example)을 참고해 서버와 빌드 환경의 실제 값을 설정한 뒤 토스용 `.ait`를 빌드합니다.
7. 콘솔에 앱 정보와 번들을 올리고 QR 실기기 테스트를 완료합니다.
8. [게임 출시 가이드](https://developers-apps-in-toss.toss.im/checklist/app-game.html)를 마지막으로 다시 확인한 뒤 검토를 요청합니다.

## 7. 출시 직전 빈칸 확인

- [ ] 최종 한글 앱 이름: `________________`
- [ ] 최종 `appName`: `penguin-bounce`
- [ ] 운영 API URL: `https://________________`
- [ ] 운영 전면 광고 그룹 ID 발급 및 `VITE_TOSS_INTERSTITIAL_AD_GROUP_ID` 설정
- [ ] 운영 번들에 테스트 광고 ID·예시값이 없는지 검증 완료
- [ ] 고객문의 이메일: `________________`
- [ ] 600×600 로고 완료
- [ ] 1932×828 썸네일 완료
- [ ] 이용약관·개인정보처리방침 공개 URL 완료
- [ ] 게임 등급분류 증빙 완료
- [ ] 영속 볼륨·백업·단일 replica 확인
- [ ] iOS 토스 QR 테스트 완료
- [ ] Android 토스 QR 테스트 완료
- [ ] 전면 광고 표시·닫기·실패 후 게임 복귀 테스트 완료
- [ ] 개인정보처리방침 v1.3 공개 및 콘솔 광고 고지 일치 확인
- [ ] 콘솔 검토 요청 전 최종 체크리스트 완료
