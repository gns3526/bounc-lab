{
  "name": "bounce-ball-community",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bounce Ball game and clear-gated community map server",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test --test-concurrency=1",
    "dev:web": "vite --mode web",
    "build:web": "vite build --mode web",
    "build:toss:dev": "node scripts/validate-toss-release.mjs --development && vite build --mode toss-dev && ait build",
    "build:toss": "node scripts/validate-toss-release.mjs && vite build --mode toss && ait build",
    "deploy:toss": "npm run build:toss && ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "3.0.2"
  },
  "devDependencies": {
    "vite": "8.2.1"
  }
}
