# Copy to android/keystore.properties. Never commit the real file or keystore.
# Relative storeFile paths are resolved from the android/ directory.
storeFile=C:/secure/path/penguin-bounce-upload.jks
storePassword=replace-with-a-secret
keyAlias=penguin-bounce-upload
keyPassword=replace-with-a-secret
